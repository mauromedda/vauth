dist: trusty
sudo: required
services:
  - docker
language: go
go:
- 1.12.4

git:
  depth: 1

script:
- make test
- make vet
- make build

branches:
  only:
  - master
matrix:
  fast_finish: true
  allow_failures:
  - go: tip
